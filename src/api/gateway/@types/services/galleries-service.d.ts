import { IGallery, IMediaType } from "@vision/core";
export declare const list: (type?: IMediaType) => Promise<IGallery[]>;
declare const _default: {
    list: (type?: IMediaType) => Promise<IGallery[]>;
};
export default _default;
